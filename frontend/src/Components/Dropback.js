import React from 'react'
import './Dropback.css'

const Dropback = ({show}) => {
    return ( show && 
        <div className="dropback">
            
        </div>
    )
}

export default Dropback
